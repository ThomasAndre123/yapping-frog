# Visitor chat widget

Future lightweight visitor-facing chat application. It should build separately
from the tenant and admin applications, expose a small bootstrap script, and
render inside an iframe or Shadow DOM boundary to isolate it from host sites.

Its production assets will live under `/widget/`; no widget routes are exposed
until an actual implementation exists.
