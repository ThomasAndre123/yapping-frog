import type { AuditEntry } from '../types';
export function AuditPage({entries}:{entries:AuditEntry[]}){return <section className="card"><div className="title-row"><div><h2>Recent activity</h2><p>Site, user, and room management events.</p></div></div><div className="audit-list">{entries.map(entry=><article key={entry.id}><div><strong>{entry.action}</strong><small>{entry.user_name??entry.user_email??'Deleted user'} · {new Date(entry.created_at).toLocaleString()}</small></div><code>{entry.target_id??entry.target_type}</code><pre>{entry.metadata==null?'No metadata':JSON.stringify(entry.metadata,null,2)}</pre></article>)}{entries.length===0&&<p className="empty">No tenant activity recorded yet.</p>}</div></section>}
