NODE_ENV=development
HOST=0.0.0.0
PORT=3000
DATABASE_URL=postgresql://chat:chat@localhost:5432/chat
REDIS_URL=redis://localhost:6379

# Enable this only on server instances that should host the administration UI.
ADMIN_ENABLED=false
ADMIN_SESSION_TTL_HOURS=8

# Optional publishable key used when an embed does not specify data-site-key.
DEFAULT_WIDGET_SITE_KEY=
